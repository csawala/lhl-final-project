<% @needs.each do |card| %>

  <div class="col s12">
    <div class="card needs">
      <div class="card-content white-text">
        <p class="needs-offers-type">
          <i class="tiny material-icons circle orange">swap_horiz</i>
          needs <%= card.goods_type.name %>
        </p><br><br>
        <p><%= card.description %></p>
      </div>
    </div>
  </div>

<% end %>